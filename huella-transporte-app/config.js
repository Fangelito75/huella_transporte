(function () {
  "use strict";

  // Después de desplegar google-apps-script/Code.gs como aplicación web,
  // pega aquí la URL terminada en /exec. No añadas claves ni contraseñas.
  window.RUTACO2_CONFIG = Object.freeze({
    apiUrl: "",
    minimumSampleSize: 5,
  });
}());
